# AutoCAD Structural Plugin - Requirements
# This plugin is designed to run within AutoCAD's Python environment
# Most dependencies are included with AutoCAD or available via .NET

# Core Python Dependencies (included with AutoCAD Python)
# These are typically available in AutoCAD's Python distribution

# External .NET Assemblies (required for AutoCAD integration)
# These are included with AutoCAD installation

# Plugin-specific dependencies that need to be bundled
# No external pip packages required as plugin runs in AutoCAD's environment

# Required .NET Framework: 4.8 or later
# Required AutoCAD Version: 2020 or later
# Required Python: 3.7+ (provided by AutoCAD)

# Development Dependencies (for building and testing)
# These are only needed during development

# Testing Framework
pytest>=6.0.0
pytest-cov>=2.10.0

# Code Quality
black>=20.8b1
flake8>=3.8.0
mypy>=0.790

# Documentation
sphinx>=3.2.0
sphinx-rtd-theme>=0.5.0

# Build Tools
setuptools>=50.0.0
wheel>=0.35.0
twine>=3.2.0

# Development Utilities
pre-commit>=2.9.0
ipython>=7.18.0
clr